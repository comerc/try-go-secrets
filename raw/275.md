В Go существуют так называемые "сырые строки" — это строки, заключенные в обратные кавычки. В них текст воспринимается буквально, без необходимости экранировать специальные символы вроде обратной косой черты, кавычек или символов новой строки. Это полезно для указания путей файлов в Windows, написания регулярных выражений или вставки многострочных текстов.  

Пример:  
```go
path := `C:\Users\temp\test.txt`
fmt.Println(path) // выведет C:\Users\temp\test.txt
```  

Таким образом, сырые строки позволяют избежать нагромождения символов `\` и делают запись более читаемой и наглядной.

```old
// path := `C:\Users\temp\test.txt` - сырые строки (raw strings) заключаются в обратные кавычки, т.е. экранирование не требуется.
```
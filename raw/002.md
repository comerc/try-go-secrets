В Go конструкция `if` может содержать так называемую инициализацию перед условием. Пример `if i := 0; i == 0 {}` сначала создает новую переменную `i`, присваивает ей значение 0, а затем проверяет условие `i == 0`. Эта переменная доступна только внутри данного блока `if` и его веток (`else if`, `else`). Такой синтаксис часто используется для компактности, например при проверке ошибки после вызова функции: переменная и условие проверяются в одной строке.  

Таким образом, главный секрет в том что перед точкой с запятой выполняется выражение инициализации, а затем в условной части можно использовать полученные значения для проверки. Это помогает сделать код чище и ограничить область видимости временных переменных.

```old
// if i := 0; i == 0 {} - присваивание; условие
```
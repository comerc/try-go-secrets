Секрет в том, что в Go при проектировании функций стоит разделять роли «поставщика» и «потребителя» кода. Если функция возвращает интерфейс, она навязывает конкретный уровень абстракции и тем самым ограничивает гибкость вызывающего кода. Гораздо практичнее вернуть конкретную реализацию, чтобы потребитель сам решил — работать напрямую с ней или обернуть более абстрактно; исключение — случаи вроде `error`, где интерфейсная форма является общеупотребимой.  

В то же время при передаче параметров функции предпочтительнее использовать интерфейсы, чтобы принимать как можно более широкий диапазон реализаций. Таким образом, мы минимизируем зависимость от конкретного типа и делаем код более универсальным и тестируемым. По сути, правило можно выразить так: «возвращай конкретное, принимай абстрактное».

```old
// В большинстве случаев возвращать (из функции/метода) лучше не интерфейсы, а конкретные реализации (error - исключение); и наоборот, функции должны принимать интерфейсы всегда, когда это возможно. (а если возвращать интерфейс, то по указателю; например, паттерн "фабричный метод"). Возврат интерфейса, как правило, ограничивает гибкость, поскольку мы заставляем всех потребителей использовать один конкретный тип абстракции.
```
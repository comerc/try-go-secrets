В Go часто используется паттерн функциональных опций для методов, где вызов вроде `tdlibClient.GetMessage(&client.GetMessageRequest{})` обозначает передачу параметров через структуру или замыкания. Вместо множества перегруженных методов или длинного списка аргументов применяется одна базовая функция, принимающая структуру с нужными настройками. Такой подход облегчает расширение API: добавление новых опций не ломает существующий код, так как старым вызовам можно оставить значения по умолчанию.  

Внутри это обычно выглядит так: создается структура `GetMessageRequest`, содержащая все возможные параметры, а сам метод `GetMessage` принимает либо её напрямую, либо набор функций-опций, которые модифицируют объект запроса. Далее метод обрабатывает уже собранный объект и выполняет вызов. Таким образом достигается удобство конфигурации и читаемость кода.  

Пример:  
```go
type GetMessageRequest struct {
    ChatID    int64
    MessageID int64
}

func (c *Client) GetMessage(req *GetMessageRequest) (*Message, error) {
    // использование req.ChatID и req.MessageID
    return &Message{}, nil
}
```

```old
// tdlibClient.GetMessage(&client.GetMessageRequest{}) паттерн опций для методов в структуре
```
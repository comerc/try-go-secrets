В Go существует механизм оборачивания ошибок: если использовать `fmt.Errorf("context: %w", err)`, то в возвращаемую ошибку встраивается исходная. Это позволяет добавить контекст и при этом сохранить возможность последующей обработки. Обратная операция выполняется через `errors.Unwrap`, которая извлекает исходную ошибку из обертки. Такая композиция ошибок удобна при построении цепочки с дополнительной информацией, сохраняя при этом возможность сравнения и проверки через функции вроде `errors.Is` или `errors.As`.  

Таким образом, `fmt.Errorf` с `%w` связывает ошибки в цепочку, а `errors.Unwrap` позволяет перемещаться по ней и доставать первопричину, что улучшает диагностику и читаемость кода в крупных проектах.

```old
// `return fmt.Errorf("bar failed: %w", err)` - оборачивание ошибки, обратная операция `errors.Unwrap`
```
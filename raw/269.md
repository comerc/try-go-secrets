`TestLogin_FailCases` — это подход в Go, когда для проверки неудачных сценариев авторизации используется табличное тестирование. Такая структура позволяет задать множество входных данных и ожидаемых ошибок, собрать их в массив или срез кейсов и прогнать через один цикл. Это повышает читаемость и уменьшает дублирование кода, так как общая логика проверки остаётся единой.  

Использование отдельных табличных тестов именно для неудачных случаев полезно тем, что такие случаи часто разнообразнее и сложнее, чем успешные сценарии — разные комбинации неверных логина и пароля, заблокированные аккаунты и т.п. Отделив их, можно лучше обобщить тесты и упростить дальнейшее расширение покрытия.

```old
// TestLogin_FailCases - применение табличных тестов отдельно для FailCases, т.к. они лучше подходят для обобщения
```
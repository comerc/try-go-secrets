В Go выражение `time.Now().Add(-diff)` используется для вычисления момента времени в прошлом. Функция `time.Now()` возвращает текущее время, а метод `Add` позволяет сместить его вперёд или назад на заданную продолжительность типа `time.Duration`. Если передать отрицательное значение, то время сдвигается назад, то есть создаётся новый объект `time.Time`, который соответствует «текущему времени минус указанная разница».  

Это удобно, например, для проверки событий, которые произошли до определённого момента, или при реализации таймаутов, когда нужно узнать, выходил ли объект за рамки заданного периода в прошлом.

```old
// time.Now().Add(-diff) - продвинуть время на заданную продолжительность
```
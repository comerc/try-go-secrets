`golang-migrate/migrate` — это инструмент и библиотека для управления миграциями базы данных в Go. Он позволяет автоматически применять или откатывать изменения схемы базы данных, сохраняя их последовательность и состояние. С его помощью можно безопасно обновлять структуру БД пошагово, что особенно полезно при работе в команде и CI/CD процессах.  

Основная идея — наличие набора файлов миграций (обычно SQL или Go), где каждая миграция имеет версию и может быть выполнена или отменена. `migrate` хранит в базе данных текущее состояние и при запуске доводит схему до нужной версии. Благодаря множеству поддерживаемых драйверов `migrate` удобно использовать с разными СУБД (Postgres, MySQL, SQLite и др.).

```old
// [migrate](https://github.com/golang-migrate/migrate)
```
В Go действительно отсутствует встроенная интерполяция строк, как в некоторых других языках, и это считается одним из частых минусов. Вместо этого разработчики используют функции `fmt.Sprintf` или конкатенацию для подстановки значений в строки. Такой подход менее удобен на первый взгляд, однако он делает код более явным и предсказуемым, убирая неочевидные преобразования и снижая риск скрытых ошибок.  

Пример:  
```go
package main

import (
	"fmt"
)

func main() {
	name := "Alice"
	age := 30
	result := fmt.Sprintf("Name: %s, Age: %d", name, age)
	fmt.Println(result)
}
```

```old
// что не нравится в Go? отсутствует интерполяция строк
```
В Go есть встроенный механизм «тестируемых примеров», который позволяет включать фрагменты кода прямо в документацию и автоматически проверять их при запуске `go test`. Такой пример показывается пользователю в godoc вместе с описанием функции или пакета и одновременно гарантирует, что демонстрируемый результат всегда согласован с реальной работой кода. Это решает проблему устаревших примеров в документации: если код изменится, тест сразу подсветит несоответствие.  

На сайте https://go.dev/blog/examples подробно объясняется, что пример оформляется как функция `Example...`, которая печатает результат. В комментариях к ней указывается ожидаемый вывод `Output: ...`. При `go test` этот вывод сравнивается с фактическим. Таким образом разработчик одновременно пишет документацию, тест и рабочий пример для пользователей.

```old
// testable examples in Go https://go.dev/blog/examples
```
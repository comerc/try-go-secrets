В Go по умолчанию каждый пакет импортируется под тем именем, которое указано в самом пакете, и изменить его можно только с помощью явного алиаса. В отличие от TypeScript, где часто используются удобные алиасы или автоматическая подстановка коротких путей, Go требует более явного стиля: если два пакета имеют одинаковое имя, то разработчик обязан задать для одного из них алиас вручную. Это с одной стороны повышает читаемость и предсказуемость кода, но с другой ограничивает гибкость и может раздражать, когда приходится часто работать с длинными путями.  

Таким образом, секрет Go тут в его философии «явное лучше, чем неявное»: вместо скрытых правил разрешения импортов язык принуждает программиста к чёткой фиксации имени пакета. Это упрощает поддержку больших проектов и делает код унифицированным, но может казаться менее удобным по сравнению с системами более свободных алиасов как в TypeScript.

```old
// что не нравится в Go? импорт без алиасов (как было в TypeScript)
```
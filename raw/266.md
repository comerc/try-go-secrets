При установке переменной окружения `GODEBUG=gctrace=1` любая Go‑программа начинает печатать в стандартный вывод подробные сообщения о работе сборщика мусора. Эти строки содержат время каждой сборки, её продолжительность и статистику памяти до и после очистки. Таким образом, чтобы «увидеть» трассировку, достаточно запустить программу или тест с указанной переменной окружения, а затем анализировать текстовый вывод в консоли.  

Пример запуска для бенчмарков:  
```bash
GODEBUG=gctrace=1 go test -bench=. -v
```  
В терминале вы получите строки, где каждая запись — это событие GC с числовыми данными. Дальше уже можно анализировать их руками или перенаправить в файл для последующего разбора.

```old
// `$ GODEBUG=gctrace=1 go test -bench=. -v` - трассировка GC; а как её посмотреть?
```
Fuzzing-тесты в Go — это способ автоматизированного тестирования, при котором программа получает случайно сгенерированные входные данные, чтобы выявить неожиданные ошибки или неисправности. В отличие от обычных юнит-тестов, где проверяются заранее определенные кейсы, тут исследуются неожиданные комбинации входов, что помогает находить редкие баги в коде. Поддержка fuzzing появилась в Go начиная с версии 1.18 непосредственно в стандартной библиотеке `testing`.  

В статье на Habr https://habr.com/ru/companies/oleg-bunin/articles/709248/ объясняется, как писать такие тесты: создается функция `FuzzXxx(f *testing.F)` где `f.Fuzz` получает комбинации входных данных. Go сам подбирает новые варианты, включая крайние значения, а также сохраняет найденные проблемные случаи для будущих прогонов. Такой подход дает сильный инструмент для повышения надежности и безопасности кода без необходимости вручную придумывать все тестовые данные.

```old
// [fuzzing-тесты](https://habr.com/ru/companies/oleg-bunin/articles/709248/)
```
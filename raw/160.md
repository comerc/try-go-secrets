Секрет в том, что директива `replace` внутри `go.mod` позволяет подменить удаленный модуль локальной копией или другой версией. Это бывает полезно при разработке, когда вы хотите тестировать изменения в соседнем модуле без публикации новой версии. Например, запись `replace example.com/username/module v0.0.0 => ../module` указывает Go использовать каталог `../module` вместо загрузки пакета с удаленного репозитория.  

Появившаяся в Go 1.18 альтернатива — команда `go work init`, которая создает workspace файл `go.work` и связывает несколько локальных модулей в одну рабочую среду. Это помогает избежать постоянного редактирования `go.mod` и более прозрачно управлять зависимостями между модулями во время локальной разработки.

```old
// replace внутри go.mod: `replace example.com/username/module v0.0.0 => ../module` (альтернатива `go work init` начиная с go1.18)
```
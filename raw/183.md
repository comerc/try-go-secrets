Закрытый канал в Go ведет себя особым образом: из него можно только читать, причём такие операции безопасны и всегда возвращают нулевое значение вместе с признаком закрытия. Но запись в закрытый канал мгновенно вызывает **panic**. В примере с `select` кажется, что закрытие канала уберегает от записи, однако это не так — `select` по-прежнему может выбрать ветку с записью в канал, потому что он не равен `nil`. Поэтому, если нужно полностью исключить возможность записи в канал после закрытия, его необходимо также сбросить в `nil`.  

Канал со значением `nil` никогда не готов к чтению или записи, поэтому все такие операции блокируются бесконечно. Благодаря этому `select` никогда не выберет ветку с записью или чтением из обнулённого канала — фактически он исключается из работы механизма выбора и становится безопасным заместителем закрытого канала.

```old
// `c := make(chan int, 1); close(c); select { case c<-1: }` - select может попытаться записать в закрытый канал, что приведёт к panic; т.е. тут недостаточно закрыть канал, нужно его сбросить в nil (c = nil)
```
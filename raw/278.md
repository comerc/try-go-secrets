Эта мысль отражает важный принцип проектирования в Go: если какой-то код сложно протестировать, значит его структура изначально нарушает принципы разделения ответственности и модульности. Тестирование в Go строится на простых интерфейсах, малых пакетах и явных зависимостях, поэтому всё, что затрудняет написание теста — признак того, что функция выполняет слишком много или имеет скрытые зависимости.  

Хорошая практика — выделять внешние взаимодействия в интерфейсы и подставлять нужные реализации в продакшн-коде и тестах. Это делает код чище, тесты проще, а архитектуру — устойчивее. Именно тестируемость является индикатором «правильного места» кода в системе.

```old
// если тебе "что-то" мешает написать тест, значит это "что-то" не на своём месте
```
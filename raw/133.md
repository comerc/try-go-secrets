В Go методы, объявленные на значении типа T, автоматически «поднимаются» и доступны также для указателя *T. Это удобно, так как один и тот же метод можно вызывать и на значении, и на ссылке. Однако если метод определен на *T, он предполагает возможность изменения значения, а значит не может быть вызван на копии T напрямую. Поэтому методы со стойкой сигнатурой `func (t *T) M()` доступны только через указатели.  

Таким образом, в повседневном коде разумно объявлять методы для *T, если они должны изменять состояние, и для T — если они реализуют действия без побочных эффектов. Это важный аспект понимания метод-сетов Go, который определяет доступность методов в разных ситуациях.

```old
// Методы, определенные для типа T, также доступны для указателей этого типа (*T). Однако обратное неверно: методы, определенные для *T, недоступны для T.
```
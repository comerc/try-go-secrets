Go модули устроены так, что для их работы не требуется централизованный реестр наподобие npm. Система Go использует концепцию модульного проксирования: по умолчанию `go` обращается к общедоступному прокси `proxy.golang.org`, но при этом можно напрямую загружать код из систем контроля версий (GitHub, GitLab и прочие). Таким образом, исходный код пакета и есть "реестр" — достаточно, чтобы он лежал в доступном репозитории.  

Это дает независимость от единого центра хранения: разработчик сам решает, где держать свой модуль и как управлять его версиями. Go инструменты автоматически кешируют, верифицируют и воспроизводимо собирают зависимости, что снижает риск "исчезновения" пакета или стороннего вмешательства.

```old
// для модулей go не нужен какой-то внешний реестр модулей (как npm в javascript, например)
```
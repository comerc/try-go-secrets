В Go есть два способа писать тесты для пакета. Первый — использовать тот же `package mypkg`, что и в тестируемом коде. Это удобно для модульных и интеграционных тестов, так как позволяет напрямую обращаться к внутренним функциям и структурам без раскрытия их в публичный API. Второй вариант — писать тесты в `package mypkg_test`. Такой подход изолирует тесты от внутренних деталей кода, проверяя только то, что экспортируется наружу, и тем самым формирует чёткие границы публичного API и улучшает его устойчивость.

Практика, описанная в секрете, советует использовать первый способ для модульных и интеграционных тестов (минимум когнитивной нагрузки, доступ к внутренностям без изменения API), а второй — для приёмочных тестов. Это позволяет одновременно легко тестировать внутренний функционал и при этом поддерживать корректное определение интерфейса модуля для внешнего использования.

```old
// для модульных/интеграционных-тестов package не менять (иначе, только ради тестов открываю все внутренности в API модуля - это неправильно, только добавляет когнитивную нагрузку в Developer Experience), а для приёмочных выносить в отдельный package с суффиксом _test, что заставляет определить API модуля.
```
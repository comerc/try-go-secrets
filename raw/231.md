В Go при использовании тегов сборки для управления выполнением тестов возникает проблема: тесты, исключенные сборкой, никак не помечаются как пропущенные и остаётся неочевидно, что они не были выполнены. Более наглядным подходом считается применение переменной окружения совместно с методом `t.Skip`. Такой способ даёт точный сигнал в отчёте тестов о том, что тест существует, но был намеренно пропущен.  

Пример:  
```go
func TestIntegration(t *testing.T) {
    if os.Getenv("INTEGRATION") != "true" {
        t.Skip("skipping integration test")
    }
    // здесь код интеграционного теста
}
```  

Таким образом, разработчик получает более прозрачное поведение: тесты отображаются в отчёте и явно говорят о своем пропуске, вместо того чтобы оставаться «невидимыми» из-за тегов сборки.

```old
// Теги сборки имеют один главный недостаток: отсутствие сигналов о том, что тест проигнорирован. Переменная среды + (testing.T).Skip - способ сообщить о пропуске теста. Например: `if os.Getenv("INTEGRATION") != "true" { t.Skip("skipping integration test") }`
```
В языке Go ассоциативный массив map устроен так, что данные хранятся в корзинах (buckets). Когда корзина переполняется (слишком много элементов с одинаковым хэшом), Go не расширяет её напрямую, а при достижении определенного коэффициента загрузки начинает процесс эвакуации во время ресайза. Ресайз создаёт новый массив корзин вдвое больше, после чего элементы старых бакетов постепенно переносятся (эвакуируются) в новые при доступе или вставке. Таким образом, перераспределение происходит постепенно, без единовременного затратного переноса всей структуры, что снижает задержки.  

Процесс эвакуации подразумевает, что часть данных ещё может оставаться в старых бакетах, пока они не будут прочитаны или изменены; в этот момент элементы копируются в новый бакет, и старый помечается освобожденным. Это позволяет эффективнее использовать память и уменьшает пиковую нагрузку на выполнение программы.

```old
// механизм эвакуации в map (про бакеты и их переполнение)
```
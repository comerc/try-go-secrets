В Go интерфейсы рекомендуется объявлять там, где они используются, а не где реализуются. Это связано с утиной типизацией: реализация автоматически удовлетворяет интерфейсу, если совпадают методы. Такой подход делает код гибче и понятнее: каждая часть системы определяет, какой функционал ей нужен, не навязывая универсальные интерфейсы.  

Однако в рамках командной разработки иногда полезно выделять общие интерфейсы в одном месте, особенно если проект сильно интегрирован и предполагает повторное использование этих типов. В случае гексагональной архитектуры интерфейсы часто выражают входные и выходные порты — то есть границы доменной логики, которые определяются на стороне бизнес‑логики, а их адаптеры реализуются уже извне. Это помогает сохранять независимость домена от технологий и конкретных реализаций.

```old
// где лучше объявлять интерфейсы: где применяются или где реализуются? (утиная типизация даёт больше свободы: для инхаус разработки выгодно все интерфейсы выделить в отдельном месте; а если реализуешь бизнес логику в гексагональной архитектуре, то лучше применять интерфейсы, как порты - т.е. на своей стороне)
```
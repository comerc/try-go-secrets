Go предоставляет набор примитивов синхронизации для безопасной работы с конкурентными горутинами и разделяемыми ресурсами. Наиболее используемый — `sync.Mutex` для эксклюзивной блокировки и `sync.RWMutex` для разделения доступа только на чтение или с записью. `sync.WaitGroup` помогает ждать завершения набора горутин, `sync.Cond` используется для координации через сигналы и условия, `sync.Once` гарантирует однократное выполнение кода, а `sync.Pool` управляет кэшированием и переиспользованием объектов. Для низкоуровневого контроля над состоянием памяти применяется пакет `sync/atomic`, обеспечивающий атомарные операции с переменными.  

Все эти примитивы позволяют балансировать между безопасностью и производительностью: простая блокировка обеспечивает надежность, а более специализированные механизмы делают возможным оптимизацию без лишних затрат.

```old
// Какие примитивы синхронизации знаете? sync.Mutex, sync.RWMutex, sync.WaitGroup, sync.Cond, sync.Once, sync.Pool, sync/atomic
```
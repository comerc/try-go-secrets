В Go можно встраивать один тип внутрь другого без имени поля — это называется анонимное поле или встраивание. В примере `type Event struct {ID int; time.Time}` в структуру `Event` встроен тип `time.Time`. Благодаря этому у `Event` автоматически появляются все методы и поля `time.Time`, а обращаться к ним можно напрямую. При инициализации `Event{ID: 1234, Time: time.Now()}` поле `Time` корректно задается значением, даже если оно было объявлено как анонимное, поскольку имя поля берется от имени типа.  

Таким образом, встраивание позволяет расширять структуры без ручного дублирования кода и делает поля/методы вложенного типа доступными «наверх», почти как наследование, но при этом сохраняется строгая композиция без скрытой магии.

```old
// type Event struct {ID int; time.Time}; event := Event{ID: 1234, Time: time.Now()} - инициализация "анонимного поля"
```
В стандартной библиотеке Go нет универсального пакета для работы с коллекциями на основе дженериков, который бы предоставлял структуры данных вроде списков, стеков или множеств для разных типов. Однако начиная с Go 1.18 появились дженерики, и на их основе можно самостоятельно писать обобщённые функции и типы. Стандартная библиотека лишь частично использует дженерики там, где это уместно, а для расширенных коллекций обычно рекомендуют сторонние решения, например https://pkg.go.dev/golang.org/x/exp/slices и https://pkg.go.dev/golang.org/x/exp/maps, где уже есть готовые утилиты работы с срезами и мапами.  

Таким образом, общих коллекций «под все» в стандартной библиотеке нет — только базовые массивы, срезы, карты и каналы, плюс экспериментальные пакеты в `x/exp`.

```old
// есть ли в стандартной библиотеке пакет для работы с коллекциями разных типов через дженерики?
```
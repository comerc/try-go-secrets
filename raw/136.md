В Go интерфейсы могут хранить значение и его конкретный тип. Операция `==` для интерфейсов допустима только тогда, когда оба значения имеют сравнимый конкретный тип. В примере `var x any = []int{1, 2, 3}` в интерфейс `any` помещается срез, а срезы в Go не сравнимы напрямую. Поэтому при выполнении `println(x == x)` произойдет паника во время выполнения. Линтеры вроде **staticcheck (SA1130)** и некоторые настройки **golangci-lint** могут подсветить такие случаи, предупреждая, что сравнение интерфейсов может привести к ошибкам, если внутри находятся несравнимые типы.  

Таким образом, рекомендация — избегать прямого сравнения интерфейсов, если не гарантировано, что динамические значения сравнимы. Вместо этого стоит использовать явные проверки длины, содержимого или специализированные функции вроде `reflect.DeepEqual`.

```old
// var x any = []int{1, 2, 3}; println(x == x) - какой линтер может отлавливать эту ситуацию? Сравнивайте значения интерфейсов, только если вы уверены, что они содержат динамические значения сравниваемых типов.
```
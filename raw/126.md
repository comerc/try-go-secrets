В Go нетипизированные (untyped) константы — это константы без фиксированного типа, которые получают значение во время компиляции и могут неявно приводиться к любому совместимому типу при использовании. Это позволяет писать более гибкий и безопасный код: одна и та же константа может без явного преобразования использоваться как `int`, `float64` или даже `complex128`, если требуется. Такой механизм упрощает работу с числами и уменьшает необходимость в ручных преобразованиях.  

Главный секрет заключается в том, что компилятор Go "растягивает" значение нетипизированной константы ровно под тот тип, с которым она взаимодействует. Это делает код универсальным, сохраняя строгость типизации во время выполнения. Например:  
```go
const x = 10
var a int = x
var b float64 = x
```  
Здесь `x` принимает тип `int` или `float64` в зависимости от контекста, при этом исходная константа остается нетипизированной.

```old
// нетипизированные константы
```
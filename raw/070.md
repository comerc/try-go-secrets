В Go тип string представляет собой неизменяемую структуру, которая хранит указатель на неизменяемый массив байтов в памяти и длину строки. При передаче строки в функцию копируется только эта структура — то есть два машинных слова: указатель и длина. Сама последовательность байтов не дублируется, поэтому операция передачи строки достаточно дешева.  

Главное отличие string от срезов заключается в том, что string не содержит дополнительного поля `cap`, поэтому занимает 16 байт (на 64-битных системах), тогда как срез — 24 байта. Иммутабельность делает возможной такую оптимизацию и обеспечивает безопасность при работе с данными.

```old
// string - это тоже структура и лежит в куче; при передаче аргументом, что копируется? строка неизменяемая. структура из двух полей - ссылка на массив байтов и len (дешевле, чем слайс - нет третьего поля cap - 16 vs 24 байт)
```
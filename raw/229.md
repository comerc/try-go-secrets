В Go при работе с `http.Client` методы `Get` и `Post` возвращают объект `*http.Response`, содержащий поток данных в поле `Body`. Этот поток необходимо закрывать с помощью метода `Close`, даже если данные не читаются полностью, чтобы освободить сетевые соединения и избежать утечек ресурсов. Если использовать `Get`, закрытие требуется после того как тело прочитано. Если использовать `Post`, то закрытие `Body` обязательно в любом случае — даже если содержимое ответа вообще не нужно, иначе соединение может зависнуть, и пул подключений клиента не будет работать корректно.

Таким образом минимальное правило: всегда вызывать `defer resp.Body.Close()` сразу после получения ответа; читать тело необходимо лишь при реальной потребности, но закрывать — всегда. Это гарантирует корректное освобождение ресурсов и повторное использование соединений HTTP.

```old
// `*http.Response.Body`, который возвращает `(http.Client).Get`, нужно закрывать после чтения методом `Close` (чтобы не оставлять утечку ресурсов); а если `(http.Client).Post` - тело ответа должно быть закрыто независимо от того, читаем ли мы его.
```
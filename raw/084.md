В Go объекты типа `sync.Mutex` инициализируются автоматически в нулевом состоянии при создании структуры, где они объявлены. Это означает, что нет необходимости явно вызывать конструктор или инициализировать поле блокировки перед использованием. Такой подход называется *ленивой инициализацией*: пока мьютекс не используется, он просто хранится в состоянии по умолчанию, а когда вызываются методы `Lock` или `Unlock`, он начинает функционировать корректно. Таким образом Go гарантирует, что даже без дополнительного кода разработчик сразу получает готовый к работе механизм синхронизации.  

Это решение упрощает код и снижает вероятность ошибок при ручной инициализации. Например:  

```go
type MyClass struct {
    lock sync.Mutex
    data int
}

func (m *MyClass) Increment() {
    m.lock.Lock()
    defer m.lock.Unlock()
    m.data++
}
```  

Здесь `lock` сразу готов к использованию без дополнительных действий.

```old
// type MyClass struct { lock sync.Mutex } - ненужно инициализировать lock, тут работает "ленивая инициализация"
```
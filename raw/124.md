Go использует UTF-8 как основную кодировку строк, что делает работу с текстом эффективной и компактной. Символы в Go представлены типом `rune` — это просто псевдоним для `int32`, чтобы подчеркнуть, что он хранит один символ Unicode, а не байт. В отличие от UTF-32 (фиксированные 4 байта на символ) и UTF-16 (2 или 4 байта), UTF-8 кодирует символ переменной длиной от 1 до 4 байтов, что экономит память и упрощает совместимость с ASCII.  

Важно понимать, что строка в Go — это последовательность байт, а не рун. Когда нужен именно символ, строка преобразуется в срез рун. Таким образом, `len(str)` возвращает количество байт, а не количество символов, что часто становится «секретом» для новичков в Go.

```old
// UTF-8 - принятая кодировка в Go (авторы Ken Thompson и Rob Pike), которая описывает рунами символы из unicode.org, где каждая руна может иметь разную длину от одного до четырех байтов, в отличии от кодировки UTF-32 (фиксированный размер int32), и UTF-16 - это кодировка фиксированной длины, которая использует два или четыре байта. но `rune // alias for int32`
```
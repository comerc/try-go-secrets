В Go при использовании пакета `encoding/json` все числовые значения, находящиеся внутри значений типа `any`, по умолчанию демаршалятся в тип `float64`. Это связано с тем, что стандарт JSON не различает целые и вещественные числа, поэтому Go выбирает универсальный тип с плавающей запятой для представления любого числа. В итоге, если выполнить `json.Unmarshal` в значение типа `map[string]any`, все числа там будут представлены как `float64`, даже если в исходном JSON было целое число.  

Чтобы сохранить точность или управлять типами чисел, можно использовать `json.Decoder` с методом `UseNumber`, либо напрямую декодировать в структуры с конкретно определёнными числовыми типами (например, `int` или `int64`). Это обходное решение позволяет избежать неожиданных преобразований и сохранить семантику числовых значений.

```old
// демаршалинг чисел any преобразуется в тип float64
```
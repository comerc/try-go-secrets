В Go сигнатура методов в интерфейсах при сравнении с реализацией учитывает только типы параметров и возвращаемых значений, а не их имена. Это означает, что метод интерфейса, у которого возвращаемые значения имеют имена, считается полностью совместимым с реализацией того же метода, у которого возвращаемые значения не именованы. Имена возвращаемых параметров — это лишь подсказка для читаемости кода, но они не участвуют в механизме сопоставления с интерфейсами.  

Таким образом, если интерфейс объявляет метод с именованными возвращаемыми значениями, структура может реализовать этот метод с такой же сигнатурой типов, но без имен. Это дает гибкость в определении интерфейсов и реализаций, сохраняя строгую типовую проверку при этом.

```old
// именованные параметры результата метода в интерфейсе совместимы с безимянной реализацией
```
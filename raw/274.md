`fmt.Scanln()` в Go используется для считывания ввода из стандартного потока до переноса строки. Она похожа на `fmt.Scan`, но в отличие от нее требует, чтобы после последнего считанного элемента обязательно шёл символ новой строки или конец файла. Это удобно, если нужно быть уверенным, что пользователь завершил ввод именно нажатием Enter, а не оставил лишние символы в буфере.  

Таким образом, `fmt.Scanln()` гарантирует более «чистый» ввод, исключая случайное чтение ненужных данных. Например:  

```go
package main

import (
    "fmt"
)

func main() {
    var name string
    fmt.Print("Введите имя: ")
    fmt.Scanln(&name)
    fmt.Println("Привет,", name)
}
```  

Здесь программа ждёт, пока не будет нажата клавиша Enter, и только тогда завершает чтение строки.

```old
// fmt.Scanln() - is similar to Scan, but stops scanning at a newline and after the final item there must be a newline or EOF.
```
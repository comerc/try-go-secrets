Секрет в том, что в Go можно выборочно отключить проверку на гонки данных для отдельных файлов с помощью build-тегов. Если внутри тестов или кода есть осознанные конструкции, которые вызывают ложноположительные срабатывания дедектора, то файл можно изъять из режима `-race` при сборке, добавив в его шапку строку `//go:build !race`. Таким образом, когда проект компилируется с включённым режимом обнаружения гонок, эти конкретные файлы просто не попадают в билды, сохраняя корректность остальных проверок.  

Это позволяет избегать "шума" при работе с тестами и сосредоточиться на реальных проблемах с гонками, при этом экономя время на анализ ненужных предупреждений. Но использовать этот приём стоит осторожно, иначе можно пропустить настоящее состояние с гонкой, если метка применена не по назначению.

```old
// если какой-то конкретный файл содержит тесты, которые приводят к гонке данных, мы можем исключить его из обнаружения гонок с помощью тега сборки `//go:build !race`
```
`examples` в Go часто используются как часть документации и автоматических тестов, где вывод функции автоматически проверяется против ожидаемого результата. Однако у этого подхода есть ограничение — поддерживать такие примеры в актуальном состоянии бывает сложно, особенно когда формат вывода меняется. Репозиторий [go-snaps](https://github.com/gkampitakis/go-snaps) решает эту проблему за счет snapshot-тестирования: вместо того чтобы явно прописывать ожидаемый вывод, он сохраняется в отдельные файлы-снимки и при изменениях можно сравнивать новый результат с сохраненным. Это упрощает ревью тестов и ускоряет разработку, делая код более гибким к рефакторингу.  

По сути, `examples` хороши для учебных целей и документации, а `go-snaps` — для практичного тестирования, где важна простота поддержки и прозрачность изменений в выводе.

```old
// но я не нашёл применения для examples, [go-snap](https://github.com/gkampitakis/go-snaps) круче.
```
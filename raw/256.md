Секрет тут в том, что `go tool pprof` умеет не только показывать срез текущего профиля, но и сравнивать два снимка кучи. Команда `$ go tool pprof -http=:8080 -diff_base heap1 heap2` запускает локальный веб‑интерфейс профайлера, где в качестве базы сравнения выбирается `heap1`, а результаты `heap2` показываются относительно него. Это помогает быстро увидеть, где именно в программе изменилась аллокация памяти между двумя профилями.  

В браузере вы получите интерактивный инструмент с графами, таблицами и визуализациями — например, можно наглядно отследить рост или уменьшение выделений памяти. Этот приём особенно полезен при исследовании утечек или регрессий в производительности, когда важно не просто видеть абсолютные значения, а понимать изменение динамики.

```old
// `$ go tool pprof -http=:8080 -diff_base heap1 heap2` - исследовать diff для кучи
```
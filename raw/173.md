В Go срез может быть равен `nil`, при этом его длина и емкость равны нулю, но сам тип остаётся корректным. Функция `append` специально реализована так, что умеет работать с `nil`-срезами: при добавлении элемента она автоматически выделяет новую память под массив и возвращает новый срез, в который добавлен требуемый элемент. Поэтому выражения вида `append(nil, x)` или `append(a, 1)`, где `a` равен `nil`, работают абсолютно так же, как и с обычным пустым срезом.

Это поведение значительно упрощает код, потому что программисту не нужно вручную проверять, был ли срез инициализирован. `append` сам корректно обрабатывает ситуацию и всегда возвращает работоспособный результат.

```old
// `var a []int; b := append(a, 1)` или `b := append([]int(nil), 1)` - append работает с nil-срезом
```
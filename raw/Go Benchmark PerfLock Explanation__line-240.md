`bench perflock` в Go используется для ограничения количества CPU, доступных бенчмарку, что позволяет создавать воспроизводимые и стабильные измерения производительности в многопроцессорных системах. Эта функция особенно полезна на Linux, где число доступных ядер может варьироваться из-за фоновых процессов. Ограничение устраняет «шум» и делает сравнение разных реализаций кода более достоверным.  

По сути, когда `perflock` активен, он управляет тем, сколько процессорных ядер Go runtime может задействовать при выполнении бенчмарка, удерживая ресурсы от конкурирующих процессов. Это гарантирует, что результаты будут отражать эффективность алгоритма, а не состояние операционной системы в момент теста.

```old
// bench perflock может ограничивать ресурсы CPU, которые может потреблять бенчмарк (только под Linux)
```
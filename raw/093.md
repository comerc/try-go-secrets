В Go при работе с каналами часто возникает необходимость ждать завершения горутины. Обычно для этого пишут <-done, чтобы заблокироваться до закрытия канала. Но можно использовать конструкцию for range done {}: она будет считывать значения из канала, пока тот не будет закрыт, и выйдет из цикла автоматически. Если в канал никто не пишет, достаточно просто закрыть его из другой горутины — цикл завершится, и мы получим такой же эффект как при <-done, но иногда код с циклом выглядит более очевидным и масштабируемым.  

Применение особенно удобно, если в будущем в канал предполагается отправлять несколько сигналов завершения, так как range легко обрабатывает все значения. Для простого однократного "сигнала готово" этот прием является более декларативным способом ожидания закрытия канала.  

Пример:  
```go
done := make(chan struct{})

go func() {
    // работа
    close(done)
}()

for range done {
    // пустое тело — просто ждем закрытия
}
```

```old
// for range done {} вместо <-done
```
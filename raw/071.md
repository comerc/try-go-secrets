В Go метод можно реализовать для структуры, указав в сигнатуре функции её тип как ресивер. При этом имя переменной ресивера (например `this`, `self` или любое другое) не обязательно должно использоваться, если оно не нужно внутри тела функции. Это удобно для методов, которые действуют как "псевдостатические" — то есть привязаны к типу, но не зависят от конкретного экземпляра.  

Пример:  

```go
package main

import "fmt"

type Bear struct{}

func (Bear) Speak() {
    fmt.Println("Roar!")
}

func main() {
    b := Bear{}
    b.Speak()
}
```  

Здесь метод `Speak` связан с типом `Bear`, но не использует сам экземпляр. Это позволяет логично группировать функции вместе с типами, сохраняя чистоту кода и не перегружая его ненужными переменными.

```old
// func (Bear) Speak() - можно не указывать "this" в рессивере при реализации метода структуры (что полезно для псевдостатических методов)
```
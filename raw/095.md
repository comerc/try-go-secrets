В Go срез является обёрткой над участком массива: он хранит указатель на первый элемент, длину и ёмкость. Однако напрямую получить доступ к самому массиву нельзя, язык скрывает это за абстракцией. Используя пакет `unsafe`, можно интерпретировать указатель на первый элемент слайса как указатель на фиксированный массив и таким образом "раскрыть" внутреннюю структуру. В приведённом примере `*(*[3]int)(unsafe.Pointer(&a[0]))` делается приведение: адрес первого элемента слайса воспринимается как указатель на массив из трёх элементов, а потом разыменовывается.  

Это сработает только если вы точно знаете длину и что за slice стоит в памяти именно этот массив. Ошибка в размере или неверное приведение ведут к неопределённому поведению. Такой подход относится к небезопасным приёмам, он не гарантирован стандартом и может нарушаться при изменениях компилятора или рантайма.

```old
// как получить доступ на внутренний массив слайса? a := []int{1,2,3}; *(*[3]int)(unsafe.Pointer(&a[0]))
```
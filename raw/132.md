В Go выражение  
```go
var _ io.Writer = (*bytes.Buffer)(nil)
```  
служит для компиляционной проверки: тип `*bytes.Buffer` действительно реализует интерфейс `io.Writer`. Здесь переменная `_` (blank identifier) используется для того, чтобы не хранить значение, а только проверить совместимость типов. Благодаря этому не создается новый объект, но компилятор гарантирует, что при изменении кода и утрате реализации интерфейса программа больше не скомпилируется.  

Таким образом, этот «трюк» — способ задокументировать и одновременно зафиксировать контракт между типом и интерфейсом, не отягощая runtime ненужными операциями и объектами.

```old
// var _ io.Writer = (*bytes.Buffer)(nil) - проверка без создания экзепляра, что *bytes.Buffer реализует интерфейс io.Writer
```
В Go стандартный пакет `flag` используется для разбора аргументов командной строки, он прост и встроен в язык, но предоставляет лишь базовую функциональность. Для более продвинутых сценариев можно использовать сторонние библиотеки: **flaggy** (≈840 звёзд на GitHub) — минималистичный и лёгкий вариант с акцентом на простоту; **pflag** (≈2200 звёзд) — совместим с `flag`, но добавляет поддержку GNU‑стиля аргументов; **go-flags** (≈2500 звёзд) — самый богатый по возможностям парсер, с автогенерацией справки и поддержкой вложенных структур.  

Таким образом, выбор зависит от потребностей: для стандартных инструментов стоит использовать `flag`, для гибкости и GNU‑совместимости — `pflag`, а для проектов с требованием расширенного интерфейса и удобной документации — `go-flags`.

```old
// flag - стандартный пакет, плюс выбор: flaggy 840 | go-flags 2500 | pflag 2200
```
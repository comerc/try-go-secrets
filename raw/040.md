В Go функция `exec.Command` принимает имя исполняемого файла и список аргументов, поэтому запись `exec.Command("ls", "-a", "-l", "-h")` полностью корректна. Когда же нужно выполнить полную строку команды так, как если бы она была введена в терминале (например `"ls -a -l -h"`), то принято запустить bash или sh с флагом `-c` и передать команду как один аргумент: `exec.Command("bash", "-c", "ls -a -l -h")`. Это позволяет использовать интерпретацию оболочки, подстановки и пайпы, что невозможно при передаче аргументов напрямую.  

Важно учитывать последствия: при `exec.Command("ls", "-a", "-l", "-h")` команда вызывает бинарь напрямую и обходит оболочку, что безопаснее и быстрее. Вариант с `bash -c` нужен только в тех случаях, когда требуется именно интерпретация shell-синтаксиса, но он потенциально менее безопасен из‑за возможности внедрения команд.

```old
// exec.Command("bash", "-c", "ls -a -l -h") - как создать полную команду в одну строку вместо exec.Command("ls", "-a", "-l", "-h")
```
В этом примере секрет в том, что аргументы функции, передаваемой в `defer`, вычисляются в момент объявления `defer`, а не во время его выполнения. Когда выполняется строка `defer func(i int) { println(i) }(i)`, значение `i` ещё равно `0`. Поэтому именно `0` и будет сохранено как аргумент для вызова этой функции при выходе из текущей области. Когда позже переменной `i` присваивается `1`, это уже не влияет на отложенный вызов, ведь он работает с копией аргумента.  

Таким образом, при завершении выполнения функции, `defer` вызовет функцию с тем значением, которое было подставлено при объявлении — `0`. Это один из часто встречающихся «подводных камней» в Go: важно помнить, что отложенные функции захватывают текущее состояние аргументов сразу.

```old
// var i int; defer func(i int) { println(i) }(i); i = 1 - выведет 0
```
В Go 1.21 появился новый пакет `slices`, который содержит удобные и обобщенные (generic) функции для работы с срезами: поиск элементов, сравнение, фильтрация, сортировка и другие операции. Раньше разработчикам приходилось писать такие утилитарные функции самостоятельно или использовать сторонние библиотеки, теперь же многое доступно "из коробки", что повышает читабельность кода и упрощает поддержку.  

Например, функцию сортировки можно вызвать так:  

```go
import "slices"

nums := []int{5, 3, 8, 1}
slices.Sort(nums) // nums станет [1,3,5,8]
```  

Появление пакета `slices` отражает развитие стандартной библиотеки Go в сторону упрощения типичных задач, делая язык более удобным без необходимости прибегать к дополнительным зависимостям.

```old
// - научиться готовить новый пакет slices (v1.21)
```
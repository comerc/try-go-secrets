Директива `//go:noinline` используется в Go для явного запрета на оптимизацию функции через "inlining" — подстановку кода функции непосредственно в место её вызова. Обычно компилятор Go решает сам, какие функции стоит встраивать, чтобы уменьшить накладные расходы на вызовы. Однако иногда разработчику нужно сохранить вызов функции как есть, например, для более прозрачного профилирования, отладки или когда важно поведение стека вызова.  

Пример:  

```go
//go:noinline
func slowAdd(a, b int) int {
    return a + b
}

func main() {
    res := slowAdd(2, 3)
    fmt.Println(res)
}
```  

Здесь функция не будет встроена в `main`, и вызов сохранится как отдельный шаг исполнения.

```old
// `//go:noinline` - запрет компилятору на inlining-оптимизацию
```
Секрет в `errWriter` заключается в том, что он оборачивает другой `io.Writer` и хранит в себе состояние ошибки. При каждой попытке записи метод `Write` сначала проверяет, не была ли уже сохранена ошибка. Если ошибка сохранена, то дальнейшие вызовы записи просто игнорируются, а прошлое значение ошибки возвращается. Благодаря этому можно писать в поток в цикле, не проверяя ошибку после каждой операции — достаточно один раз в конце посмотреть на сохранённую ошибку.  

В статье по ссылке https://habr.com/ru/articles/759920/ подробно описывается этот подход: `errWriter` позволяет элегантно упростить код, минимизируя повторяющиеся проверки ошибок и делая логику записи чище. Это полезный паттерн, когда идёт много последовательных записей в `io.Writer`.  

```go
type errWriter struct {
    io.Writer
    err error
}

func (ew *errWriter) Write(p []byte) (int, error) {
    if ew.err != nil {
        return 0, ew.err
    }
    var n int
    n, ew.err = ew.Writer.Write(p)
    return n, ew.err
}
```

```old
// ew := &errWriter{Writer: w} - где errWriter реализует свой метод Write с предварительной обработкой сохранённой ошибки https://habr.com/ru/articles/759920/
```
В Go каналы можно сравнивать только на равенство или неравенство. Два канала считаются равными, если они указывают на один и тот же экземпляр — то есть были созданы одним и тем же вызовом `make` и хранят ссылку на один объект в памяти. Также равными считаются два значения `nil` канала. В остальных случаях (например, разные вызовы `make` или один канал `nil`, а другой нет) они не равны.  

Таким образом, сравнение работает по ссылочному принципу: не содержимое очереди сравнивается, а именно указатель на конкретную структуру канала в рантайме. Это позволяет, например, безопасно проверять, инициализирован ли канал или нет.

```old
// как сравниваются каналы на == или !=: были ли два канала созданы одним и тем же вызовом функции make либо равны ли оба канала nil
```
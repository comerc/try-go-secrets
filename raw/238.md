`runtime.Goexit()` — это функция из пакета `runtime`, которая немедленно завершает текущую горутину, корректно выполняя все отложенные (`defer`) вызовы перед завершением. Она отличается от паники тем, что не вызывает восстановления (`recover`) с ошибкой: функции восстановления получают `nil`. Главная особенность в том, что если вызвать её в `main` горутине, то `main` завершится, но сама программа будет продолжать работать до тех пор, пока есть другие активные горутины; если же их больше не останется, приложение аварийно завершится.  

Это полезный инструмент для аккуратного выхода из горутины, особенно если требуется гарантированно выполнить финализацию (например: закрытие ресурсов), но при этом не обрывать выполнение всей программы или не использовать механизм паники.

```old
// runtime.Goexit() - Goexit завершает вызывающую его горутину. Никакие другие горутины не затрагиваются. Goexit выполняет все отложенные вызовы перед завершением работы горутины. Поскольку Goexit не является паникой, все вызовы восстановления в этих отложенных функциях вернут nil. Вызов Goexit из главной горутины завершает эту горутину без возврата func main. Поскольку функция func main не вернулась, программа продолжает выполнение других программ. Если все остальные горутины завершаются, то программа терпит крах.
```
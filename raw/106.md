`io.Discard` в Go — это заранее определённая переменная, реализующая интерфейс `io.Writer`, которая отбрасывает все записанные в неё данные. Она полезна, когда требуется передать корректный объект `Writer`, но фактическая запись не нужна, например, для отключения вывода в определённых режимах или для ускорения тестов, где сами данные не имеют значения.  

Таким образом, использование `io.Discard` позволяет писать код, который структурированно обрабатывает поток вывода, избегая избыточных операций записи на диск или в консоль, что делает тестирование и оптимизацию удобнее.

```old
// io.Discard - заглушка для io.Writer (например, для ускорения тестов)
```
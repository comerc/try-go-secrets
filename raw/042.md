В Go можно сделать цветные логи, используя нестандартный обработчик для slog. Идея в том, чтобы перехватывать события логгера, форматировать их под себя и добавлять ANSI-коды для выделения цветом уровней (info, warn, error). В результате при выводе в консоль сообщения будут воспринимаемо различаться по яркости и цвету, что упрощает чтение логов в режиме реального времени.  

В репозитории по ссылке реализован `slogpretty.Handler`, который оборачивает стандартный `slog.Handler` и добавляет к каждому сообщению префикс с цветом и выравниванием. Таким образом, основной механизм логирования остается тем же (встроенный slog), но вывод становится более удобным для восприятия разработчика.

```old
// цветные логи: https://github.com/GolangLessons/url-shortener/blob/c3987f66469a8d0769add18521adb9023520be95/internal/lib/logger/handlers/slogpretty/slogpretty.go
```
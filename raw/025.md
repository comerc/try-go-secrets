В статье рассматривается, что стандартный пакет regexp в Go работает не так быстро, как хотелось бы в реальных продакшен задачах, особенно при большом количестве обращений и сложных паттернах. Автор показывает, что стоит обратить внимание на альтернативные библиотеки с более высокой производительностью, например, [regexp2](https://github.com/dlclark/regexp2), которая основана на .NET-реализации и поддерживает некоторые расширенные возможности работы с регулярными выражениями, недоступные в стандартном пакете.  

Главный вывод: если в проекте критична скорость и сложность обработки строк, стандартный инструмент может быть узким местом, и нужно рассмотреть сторонние реализации. Однако в простых случаях достаточно и стандартной библиотеки, так как она более лёгкая и безопасная.

```old
// [Лучший regexp для Go](https://habr.com/ru/articles/756222/)
```
Go при вызове `time.Now()` возвращает структуру `time.Time`, в которой хранятся сразу два значения: абсолютное время (настенные часы, wall clock) и относительное время (монотонные часы, monotonic clock). Монотонные часы используются для точных вычислений разницы между моментами времени, потому что они не подвержены смещению системных часов. Однако при сериализации в JSON сохраняется только значение настенных часов, что может приводить к расхождениям при сравнении десериализованных значений.  

Чтобы избавиться от "монотонной" части и привести значение к такому виду, как оно сохраняется при маршалинге или сравнивается в базе, применяют вызов `time.Now().Truncate(0)`. Это обнуляет внутреннее поле монотонных часов, оставляя только "wall clock". Таким образом можно избежать скрытых ошибок при сравнениях времени, когда одно значение содержит монотонную часть, а другое нет.

```old
// Операционная система поддерживает два разных типа часов: настенные (wall clock) и монотонные (monotonic clock). `time.Now()` содержит оба типа часов, а маршалинг данных JSON - только "wall clock". `time.Now().Truncate(0)` - способ сбросить "monotonic clock"
```
В Go метод может быть определён для указателя на структуру, и такой метод можно вызвать даже у «nil» переменной‑указателя, если внутри метода сам доступ к памяти структуры не требуется. В приведённом примере `a` имеет тип `*MyType` и равен `nil`, но метод `Try` всё равно можно вызвать: при вызове переменная `a` просто передаётся как аргумент‑приёмник в функцию.  

Таким образом метод `Try` выполняется, получает «nil» в качестве приёмника и самостоятельно проверяет это условие. Это похоже на «статический метод класса» в других языках, но в Go это всего лишь особенность того, что методы — это функции с дополнительным первым аргументом‑приёмником.

```old
// type MyType struct {}; func (a *MyType) Try() { if a == nil { println("nil") } }; var a *MyType; a.Try(); - подобие статического метода класса
```
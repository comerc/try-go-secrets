В Go можно напрямую передавать переменные в `fmt.Println` без использования форматирования строки. Это делает код проще и понятнее, когда нужно объединить статический текст и динамические значения. Например, сообщение о завершении процесса с определенным PID можно вывести без `fmt.Sprintf`, просто перечислив аргументы через запятую.  

```go
fmt.Println("Процесс с PID", pid, "успешно завершен.")
```  

Таким образом, `fmt.Println` сам расставит пробелы между аргументами и выведет итоговую строку, что избавляет от лишних операций форматирования.

```old
// fmt.Println("Процесс с PID", pid, "успешно завершен.") - можно просто вставлять переменные без форматирования строки
```
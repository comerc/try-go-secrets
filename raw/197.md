Один из полезных «секретов» Go заключается в том, что вместо работы напрямую с файлами или строками стоит опираться на интерфейс `io.Reader`. Это позволяет абстрагироваться от источника данных: функция читает поток байт, и ей неважно, приходит ли он из файла, сети или памяти. Такой подход повышает переиспользуемость кода и облегчает тестирование, так как можно подставить любой источник, реализующий `io.Reader`.  

Пример:  

```go
package main

import (
	"fmt"
	"io"
	"strings"
)

func process(r io.Reader) {
	buf := make([]byte, 8)
	for {
		n, err := r.Read(buf)
		if n > 0 {
			fmt.Print(string(buf[:n]))
		}
		if err == io.EOF {
			break
		}
	}
}

func main() {
	process(strings.NewReader("Пример потока"))
}
```  

Здесь функция `process` ничего не знает о конкретном входе и умеет читать из любого источника данных, что делает её гибкой и удобной.

```old
// не используй имя файла в качестве входных данных функции, вместо этого применяй абстракцию io.Reader
```
В Go конструкция `val.(myType)` называется утверждением типа и используется, когда переменная имеет интерфейсный тип (например, `interface{}`). Это нужно, чтобы "снять обёртку" интерфейса и получить конкретный тип значения внутри. Если тип совпадает, операция успешна и мы получаем значение указанного типа, иначе произойдет паника.  

Чтобы избежать паники, применяют "безопасное утверждение": `v, ok := val.(myType)` — тогда `ok` вернется `true` при успешном приведении и `false` в противном случае. Таким образом, утверждение типа позволяет динамически работать с неизвестными значениями в интерфейсах, извлекая конкретные данные при необходимости.

```old
// val.(myType) - утверждение типа работает, если тип для val неопределён (т.е. interface{})
```
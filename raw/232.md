В Go при написании тестов можно использовать флаг `-short`, который активируется запуском `go test -short -v .`. В самих тестах это проверяется через функцию `testing.Short()`, и если она возвращает `true`, то можно пропускать долгие или ресурсоемкие проверки. Такой приём позволяет удобно разделять быстрые юнит-тесты и более длительные интеграционные или нагрузочные тесты.  

Пример:  
```go
func TestHeavyProcess(t *testing.T) {
    if testing.Short() {
        t.Skip("пропуск в коротком режиме")
    }
    // длительная часть теста
}
```  

Таким образом достигается гибкость: обычный локальный запуск выполняет только быстрые тесты, а полный набор со всеми затратными сценариями можно прогонять отдельно на CI или вручную.

```old
// `(testing).Short` + `go test -short -v .` - короткий режим для пропуска длительных тестов.
```
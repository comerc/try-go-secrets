В Go вызов `log.Fatal(http.ListenAndServe(...))` используется как способ завершить программу при ошибке запуска сервера. Однако у такого подхода есть тонкость: `log.Fatal` внутри себя вызывает `os.Exit(1)`, поэтому программа моментально завершается без выполнения отложенных действий и без стека ошибок, что затрудняет отладку и освобождение ресурсов.  

Более предпочтительно сначала получить ошибку от `http.ListenAndServe`, обработать её логированием или дополнительной логикой и только потом решать — завершать ли приложение или предпринимать другие шаги. Такой подход даёт больше контроля и сохраняет возможность понять контекст ошибки.

```old
// log.Fatal(http.ListenAndServe(":8080", httpserver.NewHandler())) - как вариант обработки ошибок, но профит сомнительный - мы же выходим из программы без стека ошибок (os.Exit)
```
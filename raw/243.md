Принцип локальной ссылки (locality of reference) описывает поведение программ при работе с памятью. Пространственная локализация означает, что если программа обратилась к определённой ячейке памяти, то вероятно она вскоре обратится и к соседним. Временная локализация указывает, что доступ к уже использованному участку памяти с высокой вероятностью произойдёт снова в ближайшее время. Эти свойства активно используются процессорами и кешами для ускорения доступа к данным.  

В Go это проявляется, например, при работе со срезами или циклами, где последовательные элементы массива читаются и обрабатываются подряд. Благодаря кешу доступ к элементам массива будет значительно быстрее, чем к разрозненным участкам памяти, что делает такие структуры особенно эффективными.

```old
// Принцип локальной ссылки (locality of reference): пространственная локализация кэша (будет сделано какое-то обращение к близлежащим ячейкам памяти), временная локализация кэша (к этому же месту памяти будет снова сделано какое-то обращение).
```
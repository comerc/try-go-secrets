В Go принято явно передавать контекст в каждую функцию, которая выполняет операции с возможной отменой или ограничением по времени. Это считается особенностью дизайна языка: вместо "скрытого" глобального состояния программист всегда видит и контролирует источник отмены, дедлайны и таймауты. Такой подход может показаться неудобным, так как приходится постоянно добавлять аргумент `context.Context` к функциям, даже если это усложняет сигнатуры. Но при этом достигается прозрачность и предсказуемость работы программы, а также повышается безопасность многопоточных операций.  

Ключевая идея: лучше немного больше "шума" в коде, но полная очевидность того, где и как используется контекст управления, чем скрытая логика. Это философия Go — эксплицитность ради надежности.

```old
// что не нравится в Go? требуется явную передачу контекста аргументом в функциях
```
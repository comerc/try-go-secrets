Флаг `-cpuprofile` в Go позволяет записывать данные о загрузке процессора во время выполнения тестов или бенчмарков в указанный файл. Это особенно полезно при оптимизации кода, так как можно проанализировать, какие функции занимают больше всего времени, и понять узкие места.  

Пример запуска:  
```bash
go test -bench=. -cpuprofile=cpu.prof
```  
После выполнения можно использовать `go tool pprof cpu.prof` для визуализации и анализа профиля. Это помогает разработчику наглядно увидеть распределение нагрузки и принять решение о том, какой участок кода стоит переписать.

```old
// `$ go test -bench=. -cpuprofile file` - можно включить профилировщик CPU, используя флаг -cpuprofile, например, при запуске бенчмарка.
```